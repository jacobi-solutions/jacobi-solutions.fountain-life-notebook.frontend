import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.fountainlife.notebook",
  appName: "Fountain Life Notebook",
  webDir: "build/client",
};

export default config;
